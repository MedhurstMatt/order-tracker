branch: "main"
commitMessage: "New order submission: {{fields.title}}"
pullRequestTitle: "New order submission: {{fields.title}}"
pullRequestBody: "A new order submission was received:\n\n{{fields}}"
path: "{{slug}}.json"
generatedFields:
  timestamp: !!js "new Date().toISOString()"
acceptedFields:
  - id
  - title
  - notes
  - status
  - employee
  - timestamp
